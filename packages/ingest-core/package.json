{
  "name": "@ewm/ingest-core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared ingestion machinery: parse contracts, provenance stamping, batch and freshness arbitration, snapshot serialization, and corpus replay. The only caller of resolveAlertLifecycle.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@ewm/alerts-schema": "workspace:*",
    "@ewm/core-net": "workspace:*",
    "@ewm/core-status": "workspace:*",
    "@ewm/sources": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
