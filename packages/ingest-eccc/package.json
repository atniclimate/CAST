{
  "name": "@ewm/ingest-eccc",
  "version": "0.1.0",
  "private": true,
  "description": "Pure ECCC alert parser: GeoMet feature collections (production wire) and Datamart CAP-CP XML (conformance) to NormalizedAlert via the versioned ECCC mapping table. No network code.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@ewm/alerts-schema": "workspace:*",
    "@ewm/ingest-core": "workspace:*",
    "fast-xml-parser": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
