# Winter module

**Status: stub.** Blizzard, snow, and ice hazards — ROADMAP Phase 6 (after the DDM
migration, real hydro, and the shell prove the contract).

No code yet by design. `module.manifest.json` reserves the module's identity and
hazard vocabulary so URLs, briefing sections, and platform tooling can plan around it.
When built, this module implements `HazardModule` from `@ewm/contract` like every
other — see `docs/MODULE_CONTRACT.md` and `modules/hydro` for the working example.
